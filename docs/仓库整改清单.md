# 仓库管理整改清单（民政局集中供养资金监管合规）

> 基于 2026-06-27 代码审查结论 | 项目：e-Hfnew

---

## 🔴 阶段一：合规底线（必须完成，否则过不了审计）

| # | 整改项 | 涉及文件 | 状态 |
|---|--------|---------|:----:|
| 1 | **库存表 t_stock 拆分**：UNIQUE 约束从 `material_id` 改为 `(material_id, supply_category)`，每种物资拆为 SOCIAL / CENTRALIZED 两条记录 | `schema.sql`（DDL）、`Stock.java`、`StockMapper.java` | ⬜ |
| 2 | **入库 Service 按 supply_category 写入对应库存行**：`InventoryInService.create()` 中不再查/写唯一 stock 行，改为按 `(materialId, supplyCategory)` 定位 | `InventoryInService.java` | ⬜ |
| 3 | **出库 Service 按 supply_category 扣减 + 跨类别拦截**：`InventoryOutService.create()` 按出库的 `supplyCategory` 选对应库存行扣减；SOCIAL 出库扣 CENTRALIZED 库存时直接抛异常 | `InventoryOutService.java` | ⬜ |
| 4 | **入库关联集中供养拨款批次**：`t_inventory_in` 增加 `allocation_id` 字段（FK → `t_wubao_allocation`）；入库表单增加"拨款批次"下拉 | `schema.sql`、`InventoryIn.java`、`InventoryInCreateRequest.java`、`InventoryInService.java`、`InventoryIn.vue` | ⬜ |
| 5 | **出库增加领用人/受益人字段**：`t_inventory_out` 增加 `recipient_elderly_id`、`recipient_name`、`recipient_sign_url`；出库表单增加领用人选择 + 签字上传 | `schema.sql`、`InventoryOut.java`、`InventoryOutCreateRequest.java`、`InventoryOutService.java`、`InventoryOut.vue` | ⬜ |
| 6 | **库存看板按 supply_category 分列展示**：前端库存页面改为「社会化库存」和「集中供养库存」双列或 Tab 切换 | `Stock.vue`（或对应的库存看板页面） | ⬜ |
| 7 | **盘点表 t_inventory_check 增加 supply_category**：盘点时区分类别登记 | `schema.sql`、`InventoryCheck.java`、`InventoryCheckService.java` | ⬜ |

---

## 🟡 阶段二：流程规范化（采购→入库→出库全链路）

| # | 整改项 | 说明 | 状态 |
|---|--------|------|:----:|
| 8 | **供应商主数据 t_supplier** | 新建表：名称、统一信用代码、联系人、电话、开户行、账号、在册状态 | ⬜ |
| 9 | **采购申请表 t_purchase_request** | 新建表：申请人、物资清单(JSON)、总金额、资金来源(SOCIAL/CENTRALIZED)、关联拨款批次、审批状态；复用 t_reimbursement 审批流模式 | ⬜ |
| 10 | **到货验收表 t_purchase_receipt** | 新建表：关联采购单、质检结果、验收人、实收数量，入库时从采购单拉取数据 | ⬜ |
| 11 | **入库入口改为「从采购单入库」+「直接入库」双模式** | 集中供养物资强制走采购单→验收→入库链路；社会化物资可保留直接入库 | ⬜ |
| 12 | **出库申请审批流**：领用申请 → 审批 → 出库 | 集中供养物资领用必须审批，新建 `t_outbound_request` 或复用审批框架 | ⬜ |
| 13 | **出入库列表页增加 supply_category 筛选条件** | 前端 `InventoryIn.vue`、`InventoryOut.vue` 增加筛选下拉 | ⬜ |

---

## 🟢 阶段三：ERP 化（统计、预算、对账、预警）

| # | 整改项 | 说明 | 状态 |
|---|--------|------|:----:|
| 14 | **集中供养资金看板** | 当月拨款总额 → 已采购金额 → 已领用金额 → 剩余可用余额 | ⬜ |
| 15 | **集中供养资金使用明细报表** | 民政局格式：拨款批号 → 采购物品 → 单价/数量 → 领用老人 → 金额；导出 Excel | ⬜ |
| 16 | **WarehouseCostSummary 按 supply_category 拆分** | `WarehouseFinanceController.warehouseCostSummary` 增加分类维度 | ⬜ |
| 17 | **采购预算管理 t_budget** | 按年度/月度 + 资金类型设定预算上限；采购申请时自动校验 | ⬜ |
| 18 | **库存批次追踪 t_inventory_batch** | 参照 t_drug_batch，记录批次号、有效期、入库日期 | ⬜ |
| 19 | **预警增强** | 集中供养资金使用率预警（>80%）、物资近效期预警、批复额度超标预警 | ⬜ |
| 20 | **物资消耗趋势分析** | 按老人/按类别统计月度消耗，辅助采购计划 | ⬜ |

---

## 📋 报表导出清单（民政局常用）

| # | 报表名称 | 对应建议项 | 状态 |
|---|---------|-----------|:----:|
| R1 | 集中供养资金使用明细表（按月） | #15 | ⬜ |
| R2 | 集中供养物资入库汇总表（按月/按批） | #13 + #16 | ⬜ |
| R3 | 集中供养物资出库/领用明细（含受益人） | #5 + #15 | ⬜ |
| R4 | 供应商采购汇总表（区分资金类别） | #8 | ⬜ |
| R5 | 库存分类余额表（SOCIAL vs CENTRALIZED） | #6 | ⬜ |

---

## ⚡ 执行优先级矩阵

```
        高影响
          │
    #1 #2 #3    │    #4 #5 #6
    (库存拆分)   │    (拨款关联+受益人)
          │
    ────────────┼────────────
          │
     #7 #13     │    #8 #9 #10 #11
    (盘点+筛选)  │    (采购审批链)
          │
        低影响
    低复杂度 ──────────── 高复杂度
```

**建议首周**：完成 #1–#7（阶段一全部），让系统先"分得清、扣得对、查得到"。

---

*生成时间：2026-06-27 | 下个审查节点：阶段一完成后*
