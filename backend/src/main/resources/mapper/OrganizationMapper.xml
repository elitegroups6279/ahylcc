<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hfnew.mapper.OrganizationMapper">
    <select id="countByOrgCode" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM t_organization WHERE org_code = #{orgCode}
    </select>

    <delete id="physicalDeleteByOrgCode">
        DELETE FROM t_organization WHERE org_code = #{orgCode} AND deleted = 1
    </delete>
</mapper>
